# TicTacToe Robot Requirements
# Install with: pip install -r requirements.txt

# Computer Vision
opencv-python>=4.8.0,<4.10.0
numpy>=1.21.2,<2.0.0
Pillow>=9.0.0  # For UI image display

# TFLite for piece detection
# On Raspberry Pi, use tflite-runtime (lighter):
tflite-runtime>=2.10.0
# Or on PC with full TensorFlow:
# tensorflow>=2.10.0

# Inverse Kinematics
ikpy>=3.3.0

# Note: Arm_Lib is pre-installed on the DOFBOT Raspberry Pi
# It's not available via pip - it comes with the DOFBOT image
